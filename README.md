# Apps-Script-Text-Grabber
This script takes a folder and spreadsheet as arguments. It then loops through each file with a sheets file type and copies the values in cell D72, D113 &amp; D9, while also taking the file name. It then pasts these values in the first four columns of the sheet on the next empty row.
